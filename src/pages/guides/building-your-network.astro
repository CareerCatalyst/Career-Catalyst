---
import GuidePageContainer from "../../components/GuidePageContainer.astro";
import { PAGES } from "../../utils/constants";

const {
  title = PAGES.BUILDING_YOUR_NETWORK.title,
  description = PAGES.BUILDING_YOUR_NETWORK.description,
  url = PAGES.BUILDING_YOUR_NETWORK.url,
} = Astro.props;
---

<GuidePageContainer title={title} description={description} url={url}>
  <p>HERE IS THE BODY OF THE {title} page</p>
</GuidePageContainer>
